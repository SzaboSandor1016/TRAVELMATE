package com.example.travel_mate.domain

/*
import com.example.travel_mate.ui.ViewModelMain
import com.example.travel_mate.ui.ViewModelMain.MainContent
import java.util.Stack

class SetMainContentUseCase {

    operator fun invoke(newMainContent: MainContent,currentMainContent: MainContent, prevMainContents: Stack<MainContent>): Stack<MainContent> {

        val prevContents = prevMainContents.clone() as Stack<MainContent>

        if (currentMainContent != newMainContent) {

            prevContents.push(currentMainContent)

        }

        return prevContents
    }
}*/
