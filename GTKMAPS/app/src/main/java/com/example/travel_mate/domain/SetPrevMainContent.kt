package com.example.travel_mate.domain

/*
import com.example.travel_mate.ui.ViewModelMain
import com.example.travel_mate.ui.ViewModelMain.MainContent
import java.util.Stack

class SetPrevMainContent {

    operator fun invoke(prevMainContents: Stack<MainContent>): Pair<MainContent, Stack<MainContent>>? {

        if (prevMainContents.isNotEmpty()) {

            val last = prevMainContents.pop()

            return Pair<MainContent, Stack<MainContent>>(last, prevMainContents.clone() as Stack<MainContent>)
        }

        return null
    }
}*/
